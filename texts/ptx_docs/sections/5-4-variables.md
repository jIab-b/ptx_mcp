## 5.4. Variables

In PTX, a variable declaration describes both the variableâs type and its state space. In addition to fundamental types, PTX supports types for simple aggregate objects such as vectors and arrays.
